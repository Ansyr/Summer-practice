version: '3.8'
services:
  postgres:
    container_name: "Ansyr"
    image: postgres:latest
    restart: on-failure
    environment:
      POSTGRES_DB : Bookmail
      POSTGRES_USER : postgres
      POSTGRES_PASSWORD : 1234
      POSTGRES_HOST : localhost
      POSTGRES_PORT : 12345
    ports:
      - "12345:5432"
    volumes:
      - ./dump:/var/lib/postgresql/data
